import 'dotenv/config';

/**
 * Seeds reference data: launch areas (Cúcuta, Los Patios, Villa del Rosario, …)
 * with centroids, starter categories with fallback icons, and the default
 * `system_settings` row.
 *
 * Implemented in Phase 1 once the schema exists.
 */
async function main() {
  console.log('Seed: nothing to do yet (schema lands in Phase 1).');
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
