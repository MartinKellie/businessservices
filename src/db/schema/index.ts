/**
 * Drizzle schema barrel.
 *
 * Tables are added in Phase 1 (data model & migrations). Each domain area gets
 * its own module (areas, businesses, premises, taxonomy, media, admin, enquiries,
 * settings) and is re-exported from here.
 */
export {};
