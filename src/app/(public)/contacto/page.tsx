import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function ContactPage() {
  return (
    <PlaceholderScreen
      title="Contacto"
      note="Formulario con tipos de consulta, casilla de consentimiento obligatoria, subida opcional de logo/foto. Envía a POST /api/enquiries."
    />
  );
}
