import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function SearchPage() {
  return (
    <PlaceholderScreen
      title="Resultados de búsqueda"
      note="Mapa + lista (escritorio con divisor arrastrable y paneles colapsables; móvil con conmutador mapa/lista y hoja inferior). Consume GET /api/search."
    />
  );
}
